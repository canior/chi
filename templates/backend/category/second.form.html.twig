{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-danger" href="{{ path('category_second_index', {'parentId':  parentId })}}">{{ '返回二级分类列表'|trans }}</a>
    <button class="btn btn-primary">{{ button_label|default('提交') }}</button>
{{ form_end(form) }}