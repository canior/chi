{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-danger" href="{{ path('user_stock_index', {userId: user.id}) }}">{{ '返回用户名额详情'|trans }}</a>
    <button class="btn btn-primary">{{ button_label|default('提交') }}</button>
{{ form_end(form) }}