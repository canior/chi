{{ form_start(form) }}
    {{ form_widget(form) }}
<a class="btn btn-danger" href="{{ path('offline_course_order_index') }}">{{ '返回课程订单列表'|trans }}</a>
<button class="btn btn-primary">{{ button_label|default('提交') }}</button>
{{ form_end(form) }}
