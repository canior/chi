{{ form_start(form) }}
    {{ form_widget(form) }}
<a class="btn btn-danger" href="{{ path('user_account_order_index', {'userId': user.id}) }}">{{ '返回账户管理'|trans }}</a>
<button class="btn btn-primary">{{ button_label|default('提交') }}</button>
{{ form_end(form) }}