<!DOCTYPE HTML>
<html>
<head>
    <title>{% block title %}{% if title is defined and title %}{{ title }}{% endif %}{% endblock %}</title>
    <meta name="keywords" content="West Well E"/>
    <meta name="description" content="West Well E"/>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <meta name="viewport" content="width=device-width, shrink-to-fit=no, initial-scale=1">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ realWebPath ~ '/node_modules/admin-lte/bower_components/bootstrap/dist/css/bootstrap.min.css' }}">
        <style type="text/css">
            .container {
                width: 680px;
                height: 680px;
            }

            .bg {
                background: url("{{ realWebPath ~ '/public/' ~ product.mainProductImage.file.absolutePath }}") center no-repeat;
            }

            .content {
                padding: 120px 30px 0 30px;
            }
        </style>
    {% endblock %}
</head>
<body>
<div class="container bg">
    <!-- Start: Content -->
    <div class="content">
        {% block body %}
            <img src="{{ qrCodeUri }}" width="200" height="200"/>
            <h1>Price: ${{ product.price }}</h1>
        {% endblock %}
    </div>
    <!-- End: Content -->
</div>
</body>
</html>
