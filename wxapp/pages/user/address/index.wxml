<!--pages/user/address/index.wxml-->
<view class="container">
  <view wx:for="{{addresses}}" wx:key="index">
    <view>{{item.region.fullname + ' ' + item.address}}</view>
    <view>{{item.name}} {{item.phone}}</view>
    <view class="split-line"/>
  </view>
</view>
