//手机号码中间四位显示为*号
var encode = function (array) {
  return array.length == 11 ? array.substring(0, 3) + '****' + array.substring(7) : array.substring(0, 3) + '******' + array.substring(9);
}
module.exports.encode = encode;
